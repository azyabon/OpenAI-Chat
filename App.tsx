import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  Text,
} from 'react-native';

function App(): JSX.Element {

  return (
    <SafeAreaView>
      <ScrollView>
        <Text>
          Openai Chat
        </Text>
      </ScrollView>
    </SafeAreaView>
  );
}

export default App;
